import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  formWrapper: {
    width: '100%',
    paddingLeft: '20px',
    display: 'flex',
  },
  sliderWrapper: {
    width: '25%'
  },
});

export default useStyles;
