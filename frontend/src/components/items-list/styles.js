import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  itemsRoot: {
    display: "flex",
    flexWrap: "wrap",
    width: "85%",
    marginLeft: "80px",
    height: "100%",
  },
});

export default useStyles;
